# Android Studio 诊断指南

## 🔍 请按照以下步骤收集错误信息

### 步骤 1：确认 Gradle JDK 配置

1. 打开 Android Studio
2. File → Settings → Build, Execution, Deployment → Build Tools → Gradle
3. 查看 **Gradle JDK** 设置
4. 截图或记录当前选择的 JDK 版本

### 步骤 2：尝试 Gradle Sync

1. 点击工具栏的 **Sync Project with Gradle Files** 图标（🔄）
2. 等待同步完成
3. 如果失败，查看底部的 **Build** 窗口
4. 复制完整的错误信息

### 步骤 3：查看 Gradle Console

1. 在 Android Studio 底部找到 **Build** 标签页
2. 点击左侧的 **Toggle view** 按钮
3. 选择 **Gradle Console**
4. 复制所有输出信息

### 步骤 4：尝试命令行构建（可选）

如果 Android Studio 中失败，尝试在项目目录运行：

```cmd
cd android
gradlew --version
```

这会显示 Gradle 使用的 JDK 版本。

## 🎯 常见问题排查

### 问题 1：Gradle Sync 失败

**可能原因**：
- 网络问题（无法下载依赖）
- 代理设置问题
- Gradle 缓存损坏

**解决方法**：
1. File → Invalidate Caches → Invalidate and Restart
2. 删除 `.gradle` 文件夹
3. 重新 Sync

### 问题 2：Hilt 相关错误

**错误信息示例**：
```
[Hilt] Expected @AndroidEntryPoint to have a value
```

**可能原因**：
- JDK 版本问题（已排除）
- Gradle 插件版本不匹配
- KSP 版本不匹配

**解决方法**：
检查版本兼容性

### 问题 3：KSP 错误

**错误信息示例**：
```
Error occurred in KSP
```

**可能原因**：
- KSP 版本与 Kotlin 版本不匹配
- 生成的代码路径问题

**解决方法**：
清理构建缓存

### 问题 4：依赖下载失败

**错误信息示例**：
```
Could not resolve com.google.dagger:hilt-android:2.48
```

**可能原因**：
- 网络连接问题
- Maven 仓库访问问题

**解决方法**：
检查网络连接

### 问题 5：路径问题

**错误信息示例**：
```
Unicode escape sequence
```

**可能原因**：
- 中文路径问题

**解决方法**：
使用虚拟驱动器或移动项目

## 📋 请提供以下信息

为了帮你诊断问题，请提供：

1. **Android Studio 版本**
   - Help → About

2. **Gradle JDK 设置**
   - Settings → Build Tools → Gradle → Gradle JDK

3. **完整的错误信息**
   - Build 窗口的所有输出

4. **Gradle 版本**
   - 运行 `gradlew --version` 的输出

5. **项目路径**
   - 你是从哪个路径打开的项目？
   - 原始路径还是虚拟驱动器 P:？

## 🔧 可能的解决方案

### 方案 A：清理缓存

```cmd
cd android
gradlew clean
```

然后在 Android Studio 中：
1. File → Invalidate Caches → Invalidate and Restart
2. 重新打开项目
3. Sync

### 方案 B：删除构建文件

手动删除以下文件夹：
- `android/.gradle`
- `android/app/build`
- `android/build`

然后重新 Sync

### 方案 C：检查 Kotlin 和 KSP 版本兼容性

当前配置：
- Kotlin: 1.9.20
- KSP: 1.9.20-1.0.14
- Hilt: 2.48

这些版本应该是兼容的。

### 方案 D：降级 Android Gradle Plugin

如果问题持续，可以尝试降级 AGP：

```kotlin
// android/build.gradle.kts
plugins {
    id("com.android.application") version "8.2.0" apply false  // 从 8.13.2 降级
    // ...
}
```

### 方案 E：使用 KAPT 而非 KSP（不推荐）

如果 KSP 有问题，可以临时切换回 KAPT：

```kotlin
// android/app/build.gradle.kts
plugins {
    // id("com.google.devtools.ksp")  // 注释掉
    id("kotlin-kapt")  // 添加
}

dependencies {
    // ksp("com.google.dagger:hilt-android-compiler:2.48")  // 注释掉
    kapt("com.google.dagger:hilt-android-compiler:2.48")  // 添加
}
```

## ❓ 下一步

请把你看到的具体错误信息发给我，包括：

1. 错误类型（Gradle Sync 失败？编译错误？）
2. 完整的错误堆栈
3. 你使用的 JDK 版本（在 Android Studio 中配置的）
4. 项目打开路径

这样我才能给出准确的解决方案。
